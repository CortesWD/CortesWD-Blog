{{!-- In this file you can override your pagination --}}
<nav class="pagination">
    <div class="pagination__wrap">
	    {{#if next}}
	        <a class="pagination__item pagination__item--link pagination__item--older" href="{{page_url next}}">Posts Antiguos</a>
	    {{/if}}
	   	<span class="pagination__item pagination__item--counter">{{page}} de {{pages}}</span>
	    {{#if prev}}
	        <a class="pagination__item pagination__item--link pagination__item--newer" href="{{page_url prev}}">Posts Recientes</a>
	    {{/if}}
	</div>
</nav>